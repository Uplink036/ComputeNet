apiVersion: v1
kind: Service
metadata:
  name: controller-service
  labels:
    app: controller
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  - name: api
    port: 5000
    targetPort: 5000
    protocol: TCP
  selector:
    app: controller
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: controller-ingress
spec:
  rules:
  - host: controller.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: controller-service
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: controller-service
            port:
              number: 5000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-deployment
  labels:
    app: controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: controller
  template:
    metadata:
      labels:
        app: controller
    spec:
      containers:
      - env:
        - name: DATABASE_URL
          value: mongodb://database-service:27017
        - name: FRONTEND_PORT
          value: "3000"
        name: controller
        image: uplink036/controller
        ports:
        - containerPort: 3000
          protocol: TCP
        - containerPort: 5000
          protocol: TCP
---